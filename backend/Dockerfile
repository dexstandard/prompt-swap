FROM node:22-alpine
WORKDIR /app
ENV NODE_ENV=production
ENV PORT=3000
EXPOSE 3000
COPY package*.json ./
RUN npm ci --omit=dev
COPY dist ./dist
RUN mkdir -p /data && chown -R node:node /app /data
USER node
CMD ["npm", "start"]
